Функция ПолучитьТекстФайла(ПолныйПуть) Экспорт
    
    ФС = Новый Файл(ПолныйПуть);
    Если ФС.Существует() = Ложь Тогда
        Возврат ПолныйПуть + " не существует";
    КонецЕсли;
    
    ТекстФайла = Новый ЧтениеТекста(ПолныйПуть,КодировкаТекста.UTF16);
    
    Результат = ТекстФайла.Прочитать();
    ТекстФайла.Закрыть();
    
    Возврат Результат;
КонецФункции 
